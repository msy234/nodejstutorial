module.exports.items = ['item1','item2']
// we can do that instead of const items = ...

const person = {
    name:'mustafa',
}

// and we can do something like this
module.exports.singlePerson = person